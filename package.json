{
  "name": "fake-store",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@9.8.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "tsconfig": "*",
    "turbo": "^1.13.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
